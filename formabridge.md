# formabridge — 처음으로 배포까지 이어 간 음악 서비스
> 좋아하는 음악을 기록하는 음악 SNS 서비스입니다.
> 2025.04–11 · 4인 (학부 동기) · 사이드 프로젝트

**저장소** [formalBridge/project_alpha](https://github.com/formalBridge/project_alpha) · **배포** 현재 중단

---

## 담당 범위

학부 동기 4인과 기능 단위로 나눠 맡았습니다. 전체 커밋 186건 중 53건이 제 작업입니다.

| 영역 | 작업 |
|---|---|
| 인증 | Google OAuth + JWT |
| 외부 연동 | Spotify API (참조 키 저장 + oEmbed 렌더) |
| 기능 | 팔로우 시스템, 메모 검색 |
| 화면 | 모바일 반응형 UI |
| 데이터 | 컬럼 추가 이전 레코드를 채우는 백필 배치 |

---

## 구조부터 갈라 놓고 시작했다

formabridge는 좋아하는 음악을 기록하는 음악 SNS 서비스입니다. Remix와 TypeScript를 처음 쓰는 프로젝트였고 첫 담당 작업은 메모 기능이었습니다. Remix에서는 서버에서 데이터를 읽고 쓰는 코드와 화면 코드가 같은 파일에 들어갈 수 있어서 기능 하나를 어디에 둘지부터 정해야 했습니다.

메모 기능이 동작하기 전에 `app/features/post_mokjak/` 폴더를 만들고 역할을 네 파일로 나눴습니다. `loader.tsx`는 서버에서 데이터를 읽는 요청을, `action.tsx`는 데이터를 쓰는 요청을 받았습니다. DB에 접근하는 함수는 `services.ts`에, 입력 형태는 `schema.ts`에 뒀습니다. 그 뒤 화면을 붙였습니다.

처음 쓰는 프레임워크라 서버에서 도는 코드와 화면에서 쓰는 코드를 파일부터 구분하며 시작했습니다. 이후 프로젝트의 다른 기능에도 같은 파일 구성이 반복됐고 제가 나중에 맡은 인증·팔로우·검색도 이 구조 위에 올라갔습니다.

---

## 외부 API를 인터페이스 뒤에 뒀다

곡을 검색해 결과를 화면에 보여주는 기능은 외부 API를 처음 직접 연결해 보는 작업이었습니다. 처음에는 키 없이 사용할 수 있는 MusicBrainz를 썼습니다. 외부 서비스가 보내는 데이터 모양을 그대로 화면에서 쓰면, 서비스를 바꿀 때 화면도 함께 살펴봐야 합니다.

API를 붙이면서 파일을 셋으로 나눴습니다. `IMusicSearchAPI.ts`에는 검색 조건과 결과에 필요한 제목·아티스트·앨범 등의 형태를 정의했습니다. 이렇게 코드끼리 주고받을 형태를 정한 것이 인터페이스입니다. `musicBrainz.ts`는 외부 응답을 그 형태로 바꾸고 `SearchMusic.ts`는 검색을 호출하는 역할을 맡았습니다. 화면과 서버의 읽기 요청 진입점은 `SearchMusic`을 호출하도록 뒀습니다.

넉 달 뒤 다른 팀원이 MusicBrainz를 Spotify로 교체했습니다. MusicBrainz 구현 파일은 삭제되고 Spotify 구현 파일이 새로 생겼습니다. 그 커밋에서 검색 호출부인 `SearchMusic.ts`의 변경은 3줄 추가·3줄 삭제였습니다. 화면 쪽에서는 검색 버튼 코드가 크게 삭제되고 새 파일도 추가됐습니다. 호출부와 화면의 변경 범위가 달랐습니다.

초기 인터페이스에는 MusicBrainz의 곡 식별자인 `mbid?`도 들어 있었습니다. 공통으로 쓸 형태를 정의한 파일에 특정 서비스의 식별자가 남아 있던 셈입니다. 외부 구현과 호출부를 나눴지만, 인터페이스에 남은 이 의존성은 당시 놓쳤습니다.

---

## 인증을 두 번 갈아엎었다

Google 로그인을 처음에는 클라이언트에서 처리했다가, 서버 세션으로 옮기고, 다시 JWT로 바꿨습니다. 두 번 다시 만들었고 처음 구현은 직접 지웠습니다.

되돌아보면 인증을 어디서 들고 있어야 하는지를 모르는 채로 시작한 것이 원인이었습니다. 클라이언트에 두면 서버가 요청자를 믿을 근거가 없고, 세션으로 옮기면 서버가 상태를 들고 있어야 합니다. 세 번째에서야 그 차이를 알고 골랐습니다.

---

## 남의 수정 페이지가 열렸다

프로필과 설정 화면은 `/profile/:userId/settings`처럼 URL에 사용자 번호가 들어갔습니다. 개발 중 그 번호를 다른 사용자의 번호로 바꿔 들어가 봤습니다. 다른 사람의 수정 페이지가 열렸고 수정도 됐습니다.

로그인한 사람인지 확인하는 것과 그 사람에게 수정 권한이 있는지 확인하는 것은 다른 문제였습니다. 로그인하지 않은 사람은 로그인 실패 화면으로 보내야 하지만, 이미 로그인한 사람이 남의 페이지를 열었다면 접근 권한이 없다고 처리해야 했습니다.

`jwt.server.ts`에 `requireUserOwnership` 검사 함수를 만들었습니다. 로그인 정보가 없으면 `/login/error`로 보내고 로그인한 사용자 번호와 URL의 번호가 다르면 접근 권한이 없다는 뜻의 403 응답을 반환하도록 했습니다.

이 검사를 설정·핸들 수정·오늘의 곡 추가 화면을 읽는 진입점과 프로필을 쓰는 진입점에 붙였습니다. 메모를 읽고 쓰는 진입점에도 적용했습니다. Remix의 `loader`와 `action` 양쪽에서 확인하도록 한 것입니다. URL의 사용자 번호를 받지 않던 `editHandleLoader`와 `settingsLoader`는 그 값을 받도록 함수 인자부터 바꿨습니다.

코드에 남아 있던 `// TODO: 본인 계정만 수정할 수 있도록 변경해야 함` 주석을 지우고 그 자리에 실제 소유권 검사를 넣었습니다.

---

## 팔로우는 층으로, 검색은 대상으로 나눠 올렸다

팔로우는 데이터 모델부터 화면까지 한 번에 걸리는 기능이라 모델 · 서비스 · 로더 · UI · 목록 다섯 단계로 나눠 PR을 따로 올렸습니다.

한 덩어리로 올렸으면 리뷰가 늦어졌을 것이고, 어디서 잘못됐는지 찾기도 어려웠을 겁니다. 나눠 올리니 앞 단계가 머지된 상태에서 다음 단계를 얹을 수 있었습니다.

검색 기능도 여러 PR로 나눠 올렸습니다. 팔로우가 모델·서비스·로더·UI·목록처럼 구현 층을 기준으로 나뉘었다면, 검색은 노래·사용자·메모처럼 검색 대상을 기준으로 나눴습니다. 검색창 UI와 경로 변경, 검색 뒤 오늘의 추천곡·메모로 이어지는 작업, 피드의 검색창 추가까지 7개 PR에 담았습니다. 마지막 PR에는 메모 검색 추가와 검색 코드 정리를 함께 넣었습니다. 두 기능에서 작업을 나눠 올리는 방식을 반복하되, 나누는 기준은 달리 썼습니다.

---

## Spotify 데이터를 복제하지 않았다

곡 정보를 우리 DB에 통째로 복사해두는 대신 **참조 키만 저장하고, 화면에 그릴 때 Spotify oEmbed로 받아오는 방식**을 골랐습니다. 제목·아티스트를 복제해두면 원본이 바뀌었을 때 우리 쪽이 낡은 값을 들고 있게 됩니다.

대신 표시 경로가 참조 키 유무로 갈립니다. 키가 있으면 임베드 플레이어가 뜨고, 없으면 DB에 있는 제목·아티스트를 그대로 보여줍니다. oEmbed가 돌려주는 것은 텍스트가 아니라 iframe HTML이고, 임베드를 쓴 목적도 커버·미리듣기·Spotify 연결이 붙은 재생 가능한 플레이어 위젯이었습니다.

---

## 컬럼을 추가하기 전 데이터를 채웠다

`spotifyId` 컬럼은 2025년 10월 6일(`255f8fa`)에 스키마에 추가됐습니다. 그 뒤에 등록되는 곡은 저장 시점에 값이 채워지도록 해뒀습니다. 문제는 **그 전에 이미 쌓여 있던 레코드**였습니다. 제목·아티스트만 있고 참조 키가 비어 있었습니다.

건당 외부 API 호출이 필요해 서비스 요청 흐름 안에서 처리할 수 없었습니다. 그래서 따로 한 번 돌리는 배치 스크립트로 분리했습니다. **배치 쪽이 일회성 작업입니다.**

### 먼저 출력만 해보는 모드를 만들었다

곡 제목과 아티스트로 Spotify를 검색해 결과의 키를 가져오는 방식입니다. 그런데 검색에 넣을 제목과 아티스트를 **DB에서 제대로 읽어오고 있는지**부터 확인해야 했습니다. 여기가 어긋나면 이후 단계는 볼 것도 없습니다.

DB에 쓰기 전에 바뀔 값을 확인하는 모드가 `DRY_RUN`입니다. 이 모드의 기본값을 정하는 곳은 배치를 실행하는 워크플로이고, 전달받은 값에 따라 동작을 고르는 곳은 스크립트입니다.

워크플로는 실행 입력이 없으면 `DRY_RUN`에 `'true'`를 넣습니다.

```yaml
DRY_RUN: ${{ inputs.dry_run || 'true' }}
```

이 값은 배치 컨테이너의 환경변수로 전달됩니다. 스크립트는 전달받은 값이 문자열 `'true'`일 때 DB에 쓰지 않고 변경 예정 값을 출력합니다.

```ts
const DRY_RUN = process.env.DRY_RUN === 'true';
```

```
[91/100] Processing: "예술이야" by "싸이"
[DRY] would update songId=92 spotifyId=7rPEpMvt602Np1i7TFk3Hc
[92/100] Processing: "Midnight Fiction" by "ILLIT"
...
[100/100] Processing: "Photograph" by "Ed Sheeran"
```

쓰기 없이 어떤 곡을 어떤 값으로 조회했고 무엇이 들어갈 예정인지 한 줄씩 찍어봤습니다. 맞는 걸 확인한 다음 실제로 돌렸습니다.

워크플로에서 실행 입력을 생략하면 기본값이 전달되어 DB에 쓰지 않는 모드로 실행됩니다. 이 안전망은 워크플로의 기본값 주입에 있습니다. 스크립트를 환경변수 없이 직접 실행하면 `DRY_RUN`은 `false`가 되어 실제 쓰기 경로로 들어갑니다.

### 그 외에 넣은 것

- **어느 DB에 붙었는지 먼저 확인한다** — 되돌리기 어려운 스크립트라 연결 대상을 실행 시작 시점에 출력
- **재실행이 안전하다** — 대상 조건이 `spotifyId: null`이라 이미 채워진 레코드는 자동 제외. 중단 후 재개해도 중복 갱신 없음
- **접속 정보는 마스킹한다** — `maskDatabaseUrl()`로 계정·비밀번호를 가려 출력
- **레이트리밋과 부분 실패** — 호출 간 200ms 간격, 곡 단위 `try/catch`로 한 건의 실패가 전체를 중단시키지 않게

남은 한계 — 대상 조회가 `take: LIMIT` 상한 방식이라 페이지네이션 루프가 없습니다. 실제 작업은 100건 규모라 문제가 되지 않았지만, 규모가 커지면 커서 기반 순회로 바꿔야 하는 지점입니다.

---

## 검증하려면 머지해야 하고, 머지하려면 리뷰가 필요했다

배포 워크플로는 `main` push에서만 트리거되도록 돼 있었는데, `main`은 브랜치 보호가 걸려 있어 리뷰 없이는 머지가 불가능했습니다.

```
워크플로가 main push에서만 트리거됨
  → main은 브랜치 보호 + 리뷰 필수
    → 새벽 시간대, 리뷰해줄 사람이 없음
      → 머지 불가 → 검증 불가 → 한 사이클에 몇 시간씩 대기
```

워크플로의 문법 오류, 시크릿 이름 불일치, 권한 부족은 실제로 실행해봐야만 드러납니다. 로컬 검증으로 대체되지 않습니다. 병목은 기술이 아니라 **제가 통제할 수 없는 대기 시간**이었습니다.

개인 저장소에 fork를 만들고 값을 채워 같은 조건을 재현했습니다. 브랜치 보호도 리뷰 절차도 없으니 직접 push하고 `workflow_dispatch`로 즉시 실행할 수 있었습니다.

### 30시간을 무엇이 채웠나

fork에서 워크플로 문법과 이미지 빌드는 통과시켰지만 최종 통합은 결국 팀 저장소에서 해야 했습니다. 11월 6일 저녁부터 7일 저녁까지 이틀에 걸친 작업이었습니다.

| | 값 |
|---|---|
| 커밋으로 확인되는 구간 | 22시간 35분 (2025-11-06 20:11 KST `#220` → 11-07 18:46 KST `#225`) |
| 당시 체감 | 약 30시간 |

차이는 커밋 이전의 준비·학습 시간으로 보이나 기록으로 확인되지 않아 두 값을 함께 적습니다.

Docker, Kubernetes, GitHub Actions, YAML, K8s Job을 이 작업에서 처음 다뤘습니다. 개념과 동작을 익히면서 동시에 배포를 성공시켜야 했으니 시간이 드는 것 자체는 예상된 일이었습니다. 다만 그 시간을 필요 이상으로 늘린 것은 **원인을 볼 수 있는 수단이 없었던 것**입니다. 워크플로는 템플릿에 환경변수를 치환해 매니페스트를 만드는 구조였는데, 치환 결과를 볼 수 없으니 실패의 원인이 템플릿인지 변수인지 구분할 수 없었습니다. 그래서 매번 추측으로 한 줄 고치고 다시 돌렸습니다.

```bash
envsubst < k8s/jobs/batch-job-template.yml > .out/job.yaml
echo "--- Rendered manifest ---"
cat .out/job.yaml
```

렌더된 매니페스트를 출력하는 스텝을 넣은 뒤에야 원인이 보이기 시작했습니다.

> 처음 다루는 스택이라 시간이 드는 것은 당연했지만, 관측 수단 없이 추측으로 반복한 구간은 줄일 수 있었습니다.

`main` 이력에는 검증 중이던 커밋이 그대로 남아 있습니다(#221~#225). **#225 revert는 실패 롤백이 아닙니다.** 백필은 일회성인데 `main` push마다 트리거되면 이후 모든 머지에서 재실행됩니다. 목적을 달성한 뒤 자동 트리거를 회수하고 수동 실행만 남긴 것이고, 실제로 배치가 성공한 것은 revert 이후였습니다.

---

## 지금이라면 다르게 할 것

**자격증명을 개인 저장소에 두지 않겠습니다.** 당시 fork의 Secret에 팀 클러스터의 `KUBECONFIG`를 넣어 검증했습니다. 데이터를 바꾸지 않는 모드로 돌렸지만, 팀 인프라 자격증명이 팀의 접근 통제 밖에 놓였다는 점은 잘못된 선택이었습니다. 읽기 전용 계정과 별도 네임스페이스를 준비하는 것이 맞습니다.

**로그에서 비밀값 가리는 걸 한 곳만 하지 않겠습니다.** DB 접속 문자열은 마스킹 함수를 만들어 가렸으면서 Spotify 액세스 토큰은 매 반복마다 그대로 출력하고 있었습니다. 몰라서가 아니라 한 곳에 적용하지 못한 누락이었고, 그래서 사람 주의력에 맡기면 안 되는 일이었습니다.

**AI 사용 방식을 바꿨습니다.** 이 프로젝트에서 AI는 "이 기능 추가해줘", "이 에러 고쳐줘" 식으로 썼습니다. 기능은 나왔지만 왜 그렇게 동작하는지 설명할 수 없는 코드가 남았고, 같은 문제를 다시 만나면 처음부터 다시 물어야 했습니다. 다음 프로젝트인 [PinLog](pinlog.md)에서는 반대로 접근했습니다.

**방식을 바꾼 이유가 이 프로젝트에 있습니다.**

---

## 기술 스택

Remix (React + Vite) · TypeScript · Prisma · PostgreSQL · Docker · Kubernetes · GitHub Actions (GHCR) · pnpm

---

정리 이전 판은 [`_archive/2026-08-17_formabridge_검증전원본.md`](_archive/2026-08-17_formabridge_검증전원본.md)에 보관돼 있습니다. 무엇을 왜 뺐는지는 [`_archive/README.md`](_archive/README.md) 참조.
